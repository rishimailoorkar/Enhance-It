services:
  - type: web
    name: enhance-it
    env: python
    pythonVersion: 3.10.13   # ✅ This line is missing in your current file
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
